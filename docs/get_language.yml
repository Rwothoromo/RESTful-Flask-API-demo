Endpoint that retrieves a language
---
tags:
  - Language
parameters:
  - name: name
    description: language key
    type: string
    in: path
    required: true
responses:
  200:
    description: A language
    schema:
      type: object
      properties:
        language:
          type: array
  404:
    description: Language not found
    schema:
      type: object
      properties:
        message:
          type: string
          enum:
            - "Language not found"
